start =
    expression
validchar
    = [0-9a-zA-Z_?!+-=@#$%^&*/.]

atom =
    chars:validchar+
        { return chars.join(""); }
spacedexpression =
	" " e:expression { return e; }
expression =
	atom
    / "(" a:atom s:spacedexpression* ")"
    { return [a].concat(s); }
