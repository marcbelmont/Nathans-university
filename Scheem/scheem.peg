start = expression

atom = chars:[0-9a-zA-Z_?!+=@#$%^&*/.-]+ { return chars.join(""); }

space =
  [ \t\n]
  / ";;" [^\n]*

expression = space* e:cleanexpression space* { return e; }

cleanexpression =
  atom
  / "(" e:expression+ ")" { return e; }
  / "'" e:expression { return ['quote', e]; }
